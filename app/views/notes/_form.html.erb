<%= form_with(model: [noteable, noteable.notes.build]) do |f| %>
    <div class="form-control">
      <%= f.rich_text_area :body %>
      <%= f.submit "Reply", class: "btn btn-outline-primary mt-1" %>
    </div>
<% end %>